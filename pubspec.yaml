name: flutter_radio
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audio_service: ^0.18.12
  just_audio: ^0.10.5
  audio_session: ^0.2.2
  rxdart: ^0.28.0
  shared_preferences: ^2.2.3
  wakelock_plus: ^1.1.4

flutter:
  uses-material-design: true